#ifndef BORROW_H
    #define BORROW _H
    #include <stdio.h>
	#include <stdlib.h>
	#include <time.h>
	#include <string.h>
    #include"struct.h"
    #define TAILLE 40
    #define NBBOOKSMAX 1000
void Borrow (User *s1, Borrowed *l1);

#endif
